apiVersion: apps/v1
kind: Deployment
metadata:
  name: td-whitelabelstorefront-fe-angularjs
  labels:
    app: td-whitelabelstorefront-fe-angularjs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: td-whitelabelstorefront-fe-angularjs
  template:
    metadata:
      labels:
        app: td-whitelabelstorefront-fe-angularjs
    spec:
      containers:
        - name: app
          image: td-whitelabelstorefront-fe-angularjs:17
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: td-whitelabelstorefront-fe-angularjs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: td-whitelabelstorefront-fe-angularjs
